'use strict';

import angular from 'angular';
import ShowCaseController from './showcase.controller';

export default angular.module('envicase3App.showcase', [])
  .controller('ShowCaseController', ShowCaseController)
  .name;
